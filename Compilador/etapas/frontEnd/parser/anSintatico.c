/*
 * anSintatico.c
 *
 *  Created on: 10/09/2016
 *      Authors: 
 *				Hugo Dionizio Santos
 */

#include "anSintatico.h"

// Analisadores Top-down
ParseTree topDownParsing(Token token) {
	ParseTree tree;

	printf("Analisando tokens e gerando árvore sintática via Top-down Parser...\n");

	return tree;
}

// Analisadores Bottom-up
ParseTable simplePrecedenceParser(Token token) {
	ParseTable table;

	printf("Analisando tokens e gerando árvore sintática via Simple Precedence Parser...\n");

	return table;
}

ParseTable LRParser(Token token) {
	ParseTable table;

	printf("Analisando tokens e gerando árvore sintática via LR Parser...\n");

	return table;
}

ParseTable CYK(Token token) {
	ParseTable table;

	printf("Analisando tokens e gerando árvore sintática via Algoritmo CYK...\n");

	return table;
}

ParseTable shiftReduceParser(Token token) {
	ParseTable table;

	printf("Analisando tokens e gerando árvore sintática via Shift Reduce Parser...\n");

	return table;
}

ParseTable recursiveAscedentParser(Token token) {
	ParseTable table;

	printf("Analisando tokens e gerando árvore sintática via Recursive Ascendent Parser...\n");

	return table;
}

ParseTree bottomUpParsing(Token token) {
	ParseTree tree;

	printf("Analisando tokens e gerando árvore sintática via Bottom-up Parser...\n");

	return tree;
}
